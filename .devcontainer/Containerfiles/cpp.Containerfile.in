#include "base.Containerfile"

RUN <<EOF
  eval "$(${HOMEBREW_PREFIX}/bin/brew shellenv)"

  ##// openssl needed for Boost.Cobalt
  brew install \
    cmake \
    conan \
    doxygen \
    llvm \
    mold \
    ninja \
    openssl@4 \
    sccache
EOF
