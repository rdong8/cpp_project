[project]
name = "cpp-project"
version = "0.1.0"
description = "Add your description here"
requires-python = ">=3.13"
dependencies = [
    "conan",
    "pre-commit"
]

[tool.ruff]
target-version = "py313"
